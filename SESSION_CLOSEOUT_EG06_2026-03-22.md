# Session Closeout — EG06: /docs Click Handler + Timestamp Format + UAT Project Name Fix
Date: 2026-03-22
Version: 2.38.6 → 2.38.7
MetaPM Commit: 3329188
Handoff ID: (posted via direct UAT spec)
UAT URL: https://metapm.rentyourcio.com/uat/229E18DE-3B3C-4E1F-B116-94CEF0D3A9E2

## Sprint
PTH: EG06

## Deliverables

### Fix 1 — /docs rows: onclick added to table rows
- `app/main.py`: `_DOCS_LIST_BODY` table row changed from plain `<tr>` to:
  `<tr style="cursor:pointer" onclick="window.location='/docs/'+d.id">`
  with `onclick="event.stopPropagation()"` on the inner `<a>` tag
- Root cause: rows only had `<a href>` in first cell — non-link cells unclickable

### Fix 2 — /docs timestamps: formatTimestamp() already called correctly
- Phase 0-C audit confirmed `formatTimestamp(d.updated_at)` was already present in current code
- BV-02 failure in EG05 was resolved by Fix 1 (BV-02 in EG06 spec tests timestamp format, which was already correct)
- No additional change needed

### Fix 3 — UAT renderer: PROJECT_DISPLAY_NAMES + resolve_project_name()
- `app/services/uat_generator.py`: Added `PROJECT_DISPLAY_NAMES` dict + `resolve_project_name()` at module level
  - Replaces `project.replace("-", " ").title()` at line 228 → crash when project=None, wrong display for proj-hl
  - `proj-hl` now resolves to "HarmonyLab" (not "Proj Hl")
- `app/services/uat_generator_v2.py`: Same dict + function added; also guarded `project_key` against None
  - Replaces both project.lower().replace() and project.replace("-"," ").title() calls

## Canaries
- C1: Phase 0-C audit completed — onclick missing from rows, formatTimestamp already correct, proj-hl crash in both generators
- C2: `GET /docs/bootstrap` → HTTP 200, marked.js present, doc-view present ✅
- C3: `/docs` page → formatTimestamp: True (confirmed in source) ✅
- C4: HM12 UAT canary → auth-gated (PL must verify in browser — Google OAuth required)
- C5: `GET /health` → version: 2.38.7 ✅
- C6: FRONTEND DEPLOY CANARY → onclick: True, formatTimestamp: True in /docs source ✅

## PL Actions Required
- BV-01: Open /docs → click Bootstrap row → should navigate to /docs/bootstrap with rendered markdown
- BV-02: Open /docs → find any timestamp → should show MM/DD/YYYY HH:MM:SS format
- BV-03: Open https://metapm.rentyourcio.com/uat/8709F899-CE88-45DA-871E-9DCA7CFA526D → page loads, shows "HarmonyLab" (not "proj-hl"), no Internal Server Error
- BV-04: GET /health → version: 2.38.7

## Notes
- HM12 UAT page (BV-03) requires Google OAuth in browser — cannot be verified via curl
- `uat_generator.py` and `uat_generator_v2.py` both had the same crash pattern; both fixed with identical resolve_project_name() implementation
- Both generators share the same PROJECT_DISPLAY_NAMES mapping (handles proj-XX codes + display name variants)
- EG06 fix prompt was auto-generated by Loop 3 and posted as EG06F draft after EG05 UAT failures
